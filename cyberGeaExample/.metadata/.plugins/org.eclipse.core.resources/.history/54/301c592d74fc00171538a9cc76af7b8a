package org.cyberGea.eventGenerators;

import org.eclipse.paho.client.mqttv3.IMqttDeliveryToken;
import org.eclipse.paho.client.mqttv3.MqttCallback;
import org.eclipse.paho.client.mqttv3.MqttClient;
import org.eclipse.paho.client.mqttv3.MqttConnectOptions;
import org.eclipse.paho.client.mqttv3.MqttException;
import org.eclipse.paho.client.mqttv3.MqttMessage;

public class Camera extends Sensor {
	Thread t;
	
	String name;
	int growth;
	
	
	
	public Camera(DataPublisher clientReference, int min, int max, String name) {
		
		super(clientReference,"rawData/camera",0,100);
		System.out.println("constructor");
		this.name=name;
		this.growth=min;
		measureMaturity();
		
		
	}
	/*
	 * Subscriber Part
	 */

	
	

	public void measureMaturity()
	{
		System.out.println(name + " is turn on");
		t = new Thread(this);
		
		t.start();
		
	}
	
	public void stopCamera() throws InterruptedException
	{
		this.stopThread();
		System.out.println(name + " is turn off");
	}

	

}
